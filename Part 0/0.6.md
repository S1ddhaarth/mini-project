sequenceDiagram
    participant browser
    participant server

    Note right of browser: The user clicks the submit button
    Note right of browser: The JS code adds the new note to the list and redraws the UI locally

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note left of server: { "content": "single page app post", "date": "2026-01-27" }
    server-->>browser: 201 Created
    deactivate server

    Note right of browser: No further requests; the page stays as is
